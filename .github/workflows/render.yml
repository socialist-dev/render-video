name: Youtube Video Render v2
on:
  repository_dispatch:
    types: [render_video]

jobs:
  render:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: 1. Setup Tools & Chrome
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg
          sudo curl -L https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp -o /usr/local/bin/yt-dlp
          sudo chmod a+rx /usr/local/bin/yt-dlp
          npm install puppeteer
          sudo npx puppeteer browsers install chrome --install-deps

      - name: 2. Render UI & Get Coords
        run: node render_ui.js "${{ github.event.client_payload.title }}"

      - name: 3. Download Instagram Video
        run: yt-dlp "${{ github.event.client_payload.instagram_url }}" -o "cat.mp4" --no-check-certificate

      - name: 4. Final Render (FFmpeg)
        run: |
          COORDS=$(cat coords.txt)
          X=$(echo $COORDS | cut -d: -f1)
          Y=$(echo $COORDS | cut -d: -f2)
          W=$(echo $COORDS | cut -d: -f3)
          H=$(echo $COORDS | cut -d: -f4)

          ffmpeg -i cat.mp4 -i cover.png -filter_complex \
          "[1:v]colorkey=0xFF0000:0.1:0.1[ui_mask]; \
           [0:v]scale=$W:$H:force_original_aspect_ratio=increase,crop=$W:$H,pad=1080:1920:$X:$Y:color=white[v_bg]; \
           [v_bg][ui_mask]overlay=0:0" -c:v libx264 -pix_fmt yuv420p -preset fast -c:a copy output.mp4

      # Option 1
      # - name: 5. Send Video to Telegram
      #   run: |
      #     curl -v -F "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}" \
      #     -F "video=@output.mp4" \
      #     -F "caption=✅ Render thành công!%0A📌 Tiêu đề: ${{ github.event.client_payload.title }}" \
      #     https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendVideo

      # Option 2
      # Code block below run smoothly
      # - name: 5. Upload to Telegram and Log to Sheets
      #   env:
      #     TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
      #     TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      #     GS_URL: "https://script.google.com/macros/s/AKfycbxKtdzArCSoJom9rVoozQYmEAgZrK1eeEb0QEPP0VFlZzr8cUOEr0dNwoAMiErniJ_9/exec"
      #     TITLE: ${{ github.event.client_payload.title }}
      #   run: |
      #     python3 -c "
      #     import requests, os, urllib.parse, sys
          
      #     def run():
      #         try:
      #             # Lấy thông tin từ môi trường (env)
      #             token = os.environ.get('TELEGRAM_TOKEN')
      #             chat_id = os.environ.get('TELEGRAM_CHAT_ID')
      #             title = os.environ.get('TITLE', 'No Title')
      #             gs_url = os.environ.get('GS_URL')

      #             if not token or not chat_id:
      #                 print('❌ Thiếu Secret TELEGRAM_TOKEN hoặc TELEGRAM_CHAT_ID')
      #                 sys.exit(1)

      #             # 1. Upload video lên Telegram
      #             print('--- Uploading to Telegram ---')
      #             with open('output.mp4', 'rb') as f:
      #                 r_tg = requests.post(
      #                     f'https://api.telegram.org/bot{token}/sendVideo',
      #                     data={'chat_id': chat_id, 'caption': '✅ Rendered: ' + title},
      #                     files={'video': f}
      #                 )
                  
      #             if r_tg.status_code != 200:
      #                 print(f'❌ Telegram Error: {r_tg.text}')
      #                 sys.exit(1)
                  
      #             # Lấy file_id
      #             file_id = r_tg.json()['result']['video']['file_id']
      #             print(f'✅ File ID thành công: {file_id}')
          
      #             # 2. Ghi file_id vào Google Sheets
      #             print('--- Logging to Google Sheets ---')
      #             title_enc = urllib.parse.quote(title)
      #             file_id_enc = urllib.parse.quote(file_id)
                  
      #             final_gs_url = f'{gs_url}?title={title_enc}&url={file_id_enc}'
      #             r_gs = requests.get(final_gs_url)
      #             print(f'✅ Google Sheets response: {r_gs.text}')
                  
      #         except Exception as e:
      #             print(f'❌ Lỗi Python: {str(e)}')
      #             sys.exit(1)
      #     run()
      #     "

      # Option 3
      - name: 5. Upload Video, Thumbnail and Log to Sheets
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          GS_URL: "https://script.google.com/macros/s/AKfycbxKtdzArCSoJom9rVoozQYmEAgZrK1eeEb0QEPP0VFlZzr8cUOEr0dNwoAMiErniJ_9/exec"
          TITLE: ${{ github.event.client_payload.title }}
        run: |
          python3 -c "
          import requests, os, urllib.parse, sys
          
          def run():
              try:
                  token = os.environ.get('TELEGRAM_TOKEN')
                  chat_id = os.environ.get('TELEGRAM_CHAT_ID')
                  title = os.environ.get('TITLE', 'No Title')
                  gs_url = os.environ.get('GS_URL')

                  # 1. Upload VIDEO lên Telegram (Lấy file_id để n8n tải)
                  print('--- Uploading Video to Telegram ---')
                  with open('output.mp4', 'rb') as f:
                      r_v = requests.post(f'https://api.telegram.org/bot{token}/sendVideo', 
                          data={'chat_id': chat_id}, files={'video': f})
                  video_file_id = r_v.json()['result']['video']['file_id']

                  # 2. Upload ẢNH lên Telegra.ph (Để lấy URL ảnh công khai)
                  # Dịch vụ này của Telegram nên cực kỳ ổn định và không chặn GitHub
                  print('--- Uploading Thumbnail to Telegra.ph ---')
                  with open('cover.png', 'rb') as f:
                      # Telegra.ph yêu cầu cấu trúc file cụ thể
                      files = {'file': ('cover.png', f, 'image/png')}
                      r_img = requests.post('https://telegra.ph/upload', files=files)
                  
                  # Telegra.ph trả về mảng JSON, lấy phần tử đầu tiên
                  res_img = r_img.json()
                  if isinstance(res_img, list) and 'src' in res_img[0]:
                      thumb_url = 'https://telegra.ph' + res_img[0]['src']
                  else:
                      print(f'❌ Telegra.ph Error: {res_img}')
                      thumb_url = 'Upload Failed'
                  
                  print(f'✅ Video ID: {video_file_id}')
                  print(f'✅ Thumb URL: {thumb_url}')
          
                  # 3. Gửi sang Google Sheets
                  print('--- Logging to Sheets ---')
                  params = {
                      'title': title,
                      'url': video_file_id,
                      'thumb': thumb_url
                  }
                  final_url = gs_url + '?' + urllib.parse.urlencode(params)
                  r_gs = requests.get(final_url)
                  print(f'✅ Google Sheets response: {r_gs.text}')
                  
              except Exception as e:
                  print(f'❌ Error: {str(e)}')
                  sys.exit(1)
          run()
          "
    
